node_modules
dist
preview-build
.turbo
pnpm-lock.yaml

# Die Dokumentation wird von Hand gesetzt. Prettier würde Tabellen neu
# ausrichten und Absätze umbrechen, was jede inhaltliche Änderung hinter
# Formatierungsrauschen verstecken würde.
*.md

# Dasselbe gilt für die Workflows und Vorlagen: dort stehen Prüfskripte in
# Blockskalaren, und das Gerüst drumherum umzuschreiben bringt nichts außer
# unlesbaren Diffs an genau den Stellen, die man lesen können muss.
.github/

# Die Migrationen erzeugt drizzle-kit, den Ordner meta schreibt es bei jedem
# Lauf neu. Wer ihn formatiert, bekommt ihn beim nächsten generate wieder
# umformatiert zurück.
packages/server/migrations/
