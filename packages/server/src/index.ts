// NestJS modules, Drizzle repositories, auth and the sync endpoints live here.
// The contents arrive with the issues of phase 0; this entry point exists so
// the package has something to type check and export.
export {}
