packages:
  - packages/*
  # Trades are packages of their own under ADR 0008. The folder comes with the
  # first trade; until then this pattern matches nothing and does no harm.
  - packages/gewerke/*

# pnpm holds back freshly published versions until they have been out for a
# while. That is the protection against a hijacked npm account, where the
# malicious version is usually found out and withdrawn within hours. Whoever
# needs a version still inside that window enters it under
# minimumReleaseAgeExclude. So far that was never necessary: the rule "newest
# possible version" costs at most a day because of it, and that is no reason
# to switch the protection off.

# pnpm does not run the install scripts of other packages by itself. That is
# the protection against a package that does something other than installing
# while it installs. esbuild comes in through drizzle-kit and does not need
# its script: its binary sits in an optional package per platform, and
# drizzle-kit generates migrations without it. Checked on 18.09.2026.
allowBuilds:
  esbuild: false
